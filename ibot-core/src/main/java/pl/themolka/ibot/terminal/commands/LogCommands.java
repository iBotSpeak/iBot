package pl.themolka.ibot.terminal.commands;

import pl.themolka.ibot.IBot;
import pl.themolka.ibot.command.CommandContainer;

public class LogCommands extends CommandContainer {
    public LogCommands(IBot iBot) {
        super(iBot);
    }
}
